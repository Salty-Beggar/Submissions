
#include <algorithm>
#include <cstdio>
#include <iostream>
using namespace std;

const long int MAX_M = 1000000000;
// long int n, arr[MAX_M];

int main() {
    // freopen("pairup.in", "r", stdin);
    // freopen("pairup.out", "w", stdout);
    // cin >> n;

    // long int index = 0;
    // for (long int i = 0; i < n; i++) {
    //     long int count, value;
    //     cin >> count >> value;
    //     for (long int j = 0; j < count; j++) {
    //         arr[index++] = value;
    //     }
    // }
    // long int m = index;
    // sort(arr, arr+m);
    // long int maxTime = 0;
    // for (long int i = 0; i < m/2; i++) {
    //     long int r = m-i-1;
    //     maxTime = max(arr[i]+arr[r], maxTime);
    // }
    // cout << maxTime;
    return 0;
}
